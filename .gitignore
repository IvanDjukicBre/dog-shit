__pycache__/
*.pyc
.DS_Store
.dog-shit/
dist/

# the marketing site is not part of this project -- it lives here only so it
# can still be deployed to Cloudflare Pages, which uploads it directly
site/
serve.py
serve.log
.claude/launch.json

# wrangler local cache (contains account id and email)
.wrangler/
